# Base URL of the API server (defaults to http://localhost:3000 if unset)
VITE_API_URL=http://localhost:3000
# OAuth 2.0 Client ID from the Google Cloud Console (must match the server's GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
